namespace _25_classes
{
    internal class Program
    {
        static void Main(string[] args)
        {
            // classes => A bundle of code can be used as a blueprint
            // to create object.
            // static class => object not necessary to create class to show helper
            //method.
            messageClass.Hello();
            messageClass.Waiting();
            messageClass.Bye();
        }
    }
}
